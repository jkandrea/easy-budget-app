name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      # 빌드 시점에 REACT_APP_ 로 주입하면 번들에서 process.env.REACT_APP_...로 사용 가능
      REACT_APP_FIRE_API_KEY: ${{ secrets.FIRE_API_KEY }}
      REACT_APP_FIRE_AUTH_DOMAIN: ${{ secrets.FIRE_AUTH_DOMAIN }}
      REACT_APP_FIRE_PROJECT_ID: ${{ secrets.FIRE_PROJECT_ID }}
      REACT_APP_FIRE_STORAGE_BUCKET: ${{ secrets.FIRE_STORAGE_BUCKET }}
      REACT_APP_FIRE_MESSAGING_SENDER_ID: ${{ secrets.FIRE_MESSAGING_SENDER_ID }}
      REACT_APP_FIRE_APP_ID: ${{ secrets.FIRE_APP_ID }}
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci
        # run: npm install

      - name: Build React App
        run: npm run build --if-present

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          projectId: ${{ secrets.FIREBASE_PROJECT_ID }}
          channelId: live
